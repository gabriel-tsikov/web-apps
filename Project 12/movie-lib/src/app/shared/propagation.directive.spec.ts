import { PropagationDirective } from './propagation.directive';

describe('PropagationDirective', () => {
  it('should create an instance', () => {
    const directive = new PropagationDirective();
    expect(directive).toBeTruthy();
  });
});
